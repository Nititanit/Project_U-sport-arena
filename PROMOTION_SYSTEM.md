# ระบบโปรโมชั่น (Promotion System) 📋

## การตั้งค่าฐานข้อมูล

### 1. สร้างตาราง Promotions ใน Supabase

รัน SQL ต่อไปนี้ใน Supabase SQL Editor:

```sql
-- Create promotions table
CREATE TABLE IF NOT EXISTS promotions (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name VARCHAR(255) NOT NULL UNIQUE,
  description TEXT,
  discount_percentage DECIMAL(5,2),
  discount_amount DECIMAL(10,2),
  valid_from TIMESTAMP WITH TIME ZONE NOT NULL,
  valid_until TIMESTAMP WITH TIME ZONE NOT NULL,
  status VARCHAR(50) DEFAULT 'active' CHECK (status IN ('active', 'inactive')),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Insert sample promotions
INSERT INTO promotions (name, description, discount_percentage, discount_amount, valid_from, valid_until, status) VALUES
('SUMMER200', 'ส่วนลดฤดูร้อน 200 บาท', NULL, 200.00, '2025-01-01 00:00:00+00', '2026-12-31 23:59:59+00', 'active'),
('WELCOME20', 'ส่วนลด 20% สำหรับสมาชิกใหม่', 20.00, NULL, '2025-01-01 00:00:00+00', '2026-12-31 23:59:59+00', 'active'),
('VIPUSER500', 'ส่วนลด 500 บาทสำหรับสมาชิก VIP', NULL, 500.00, '2025-01-01 00:00:00+00', '2026-12-31 23:59:59+00', 'active'),
('EXPIRED2024', 'โปรโมชั่นที่หมดอายุแล้ว', NULL, 300.00, '2023-01-01 00:00:00+00', '2024-12-31 23:59:59+00', 'active')
ON CONFLICT (name) DO NOTHING;

-- Create index for faster lookups
CREATE INDEX IF NOT EXISTS idx_promotions_name ON promotions(name);
CREATE INDEX IF NOT EXISTS idx_promotions_status_valid ON promotions(status, valid_from, valid_until);
```

### 2. ตรวจสอบการตั้งค่า

ระบบจะทำงานในโหมดต่อไปนี้:
- **Production Mode**: Query จากฐานข้อมูล Supabase จริง
- **Fallback Mode**: ถ้า database ไม่พร้อมใช้งาน จะใช้ mock data แทน

ไม่จำเป็นต้อง restart server ระบบจะปรับตัวเองอัตโนมัติ

## ฟีเจอร์หลัก

### 1️⃣ ประเภทส่วนลด
- **จำนวนเงินคงที่**: เช่น ลด 200 บาท (ทั้งหมด)
- **ร้อยละ**: เช่น ลด 20% ของราคา

### 2️⃣ ระบบตั้งเวลา
- กำหนดวันที่เริ่มต้นและสิ้นสุดของโปรโมชั่น
- โปรโมชั่นจะใช้ได้เฉพาะในช่วงเวลาที่กำหนด

### 3️⃣ ระบบการใช้งาน
ผู้ใช้สามารถ:
- ใส่รหัสโปรโมชั่นในหน้าจองสนาม
- เห็นส่วนลดแบบเรียลไทม์
- ยกเลิกการใช้โปรโมชั่นได้ตลอดเวลา

## ไฟล์และโครงสร้าง

### 📄 ไฟล์หลัก

#### 1. `lib/promotions.ts` - Promotion Service
```typescript
// ฟังก์ชันหลัก:
calculateDiscount(basePrice, promotion)     // คำนวณจำนวนส่วนลด
calculateFinalPrice(basePrice, promotion)   // คำนวณราคาสุดท้าย
isPromotionValid(promotion)                 // ตรวจสอบความถูกต้อง
getPromotionDisplayText(promotion)          // ข้อความแสดงส่วนลด
searchPromotionByCode(code)                 // ค้นหาโปรโมชั่นตามรหัส
mockPromotions                              // ข้อมูล mock สำหรับ testing
```

#### 2. `components/PromotionInput.tsx` - UI Component
- แสดงฟอร์มสำหรับใส่รหัสส่วนลด
- แสดงสถานะเมื่อใช้โปรโมชั่นสำเร็จ
- แสดงข้อความ error เมื่อรหัสไม่ถูกต้อง

#### 3. `app/reservation/page.tsx` - Reservation Page (อัปเดต)
- รวม PromotionInput component
- แสดงราคาเดิม + ส่วนลด + ราคาสุดท้าย
- บันทึก promotion_id เมื่อทำการจอง

#### 4. `app/admin/promotions/page.tsx` - Admin Management Page
- สร้างโปรโมชั่นใหม่
- แก้ไขสถานะ (เปิด/ปิดใช้งาน)
- ลบโปรโมชั่น
- ดูรายการโปรโมชั่นทั้งหมด

## วิธีการใช้

### สำหรับผู้ใช้ 👤

1. ไปที่หน้าจองสนาม (`/reservation`)
2. เลือกเวลาที่ต้องการ
3. ดูราคารวม
4. ใส่รหัสส่วนลดในช่อง "มีรหัสส่วนลด?"
5. กดปุ่ม "ใช้"
6. เห็นราคาลดลง
7. กดยืนยันการจอง

**รหัสสำหรับทดสอบ:**
- `SUMMER200` - ลด 200 บาท
- `WELCOME20` - ลด 20%
- `VIPUSER500` - ลด 500 บาท

### สำหรับผู้ดูแลระบบ 👨‍💼

1. ไปที่หน้า Admin: `/admin/promotions`
2. กดปุ่ม "+ เพิ่มโปรโมชั่นใหม่"
3. กรอกข้อมูล:
   - **รหัสโปรโมชั่น**: ตัวอักษร และตัวเลข (เช่น SUMMER200)
   - **คำอธิบาย**: ตัวอักษร (เช่น "ส่วนลดฤดูร้อน")
   - **ประเภทส่วนลด**: บาท หรือ ร้อยละ
   - **จำนวนส่วนลด**: ตัวเลข (เช่น 200 หรือ 20)
   - **วันที่เริ่มต้น**: เลือกวันที่
   - **วันที่สิ้นสุด**: เลือกวันที่
   - **สถานะ**: เปิดใช้งาน/ปิด
4. กดปุ่ม "บันทึกโปรโมชั่น"
5. สามารถเปิด/ปิด หรือลบได้ภายหลัง

## ตัวอย่างการใช้

### ตัวอย่าง 1: ส่วนลด 200 บาท
```
ราคารวม: 2,000 บาท
โปรโมชั่น: SUMMER200
ส่วนลด: 200 บาท
ราคาสุดท้าย: 1,800 บาท
```

### ตัวอย่าง 2: ส่วนลด 20%
```
ราคารวม: 1,000 บาท
โปรโมชั่น: WELCOME20
ส่วนลด: 200 บาท (20% ของ 1,000)
ราคาสุดท้าย: 800 บาท
```

## การเชื่อมต่อกับ Supabase (ขั้นต่อไป)

ในปัจจุบัน ระบบใช้ mock data สำหรับ testing หรือสามารถปรับปรุงโดย:

1. **สร้าง Table `promotions` ใน Supabase**:
```sql
CREATE TABLE promotions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL UNIQUE,
  description TEXT,
  discount_percentage DECIMAL,
  discount_amount DECIMAL,
  valid_from TIMESTAMP,
  valid_until TIMESTAMP,
  status TEXT DEFAULT 'active',
  created_at TIMESTAMP DEFAULT NOW()
);
```

2. **อัปเดท `lib/promotions.ts`**:
```typescript
export async function searchPromotionByCode(code: string): Promise<Promotion | null> {
  const supabase = createClient()
  const { data } = await supabase
    .from('promotions')
    .select('*')
    .eq('name', code.toUpperCase())
    .single()
  
  return data || null
}
```

3. **อัปเดท `app/admin/promotions/page.tsx`**:
- เปลี่ยนจาก mock data เป็นการดึงจาก Supabase
- บันทึกข้อมูลไปยัง Supabase แทนที่จะเก็บใน state

## ความหมายของ Status

| Status | ความหมาย | ใช้ได้หรือไม่ |
|--------|---------|-------------|
| active | เปิดใช้งาน | ✅ ใช้ได้ (ถ้าไม่หมดอายุ) |
| inactive | ปิดใช้งาน | ❌ ไม่ใช้ได้ |
| (หมดอายุ) | เกินวันสิ้นสุด | ❌ ไม่ใช้ได้ |

## TypeScript Interfaces

```typescript
interface Promotion {
  id: string
  name: string                      // รหัสโปรโมชั่น
  description: string | null        // คำอธิบาย
  discount_percentage: number | null // ส่วนลด %
  discount_amount: number | null    // ส่วนลดเป็นบาท
  valid_from: string               // วันที่เริ่มต้น (ISO)
  valid_until: string              // วันที่สิ้นสุด (ISO)
  status: string                   // 'active' หรือ 'inactive'
}
```

## สิ่งที่ต้องพัฒนาต่อไป

- [ ] เชื่อมต่อ Supabase Database
- [ ] เพิ่ม Authentication สำหรับ Admin เท่านั้น
- [ ] สร้าง Analytics (ดูจำนวนการใช้โปรโมชั่น)
- [ ] เพิ่มฟังก์ชัน: กำหนดจำนวนการใช้งาน (ขีดจำกัด)
- [ ] เพิ่มฟังก์ชัน: ใช้ได้กับฟิลด์บางฟิลด์เท่านั้น
- [ ] ระบบ Combination: ใช้หลายโปรโมชั่นพร้อมกัน

## ผู้พัฒนา
ระบบสร้างด้วย Next.js + TypeScript + Tailwind CSS
